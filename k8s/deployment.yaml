apiVersion: apps/v1
kind: Deployment
metadata:
  name: pol-google-space-jsm-webhook
  namespace: pol-google-space-jsm-webhook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pol-google-space-jsm-webhook
  template:
    metadata:
      labels:
        app: pol-google-space-jsm-webhook
    spec:
      containers:
        - name: pol-google-space-jsm-webhook
          image: europe-west2-docker.pkg.dev/rise-sandbox/pol-google-space-jsm-webhook/latest
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "8080"
          envFrom:
            - secretRef:
                name: google-space-jsm-webhook-secrets